<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.sample.keiba">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CalcActivity"
            android:label="@string/title_activity_calc"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".UserInfoViewPagerAdapter" />
        <activity android:name=".TotalActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ArrayListAdapter" />
        <activity android:name=".AccountActivity"></activity>
    </application>

</manifest>